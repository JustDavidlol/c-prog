#include <stdio.h>
#include <math.h>


int main() 
{

     float a=6, b=7, c=10, s, area;

     s = (a+b+c)/2;
     area = sqrt( s*(s-a)*(s-b)*(s-c) );

     printf("area of a triangle is %.2f",area);

     return 0;
}
